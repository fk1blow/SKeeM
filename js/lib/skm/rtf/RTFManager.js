
// RTF Manager implementation

define(['skm/k/Object',
  'skm/util/Logger',
  'skm/util/Subscribable'],
  function(SKMObject, SKMLogger, Subscribable)
{
'use strict';


var Logger = SKMLogger.create();


// handles the ConnectorManager
// creates the connectors
// and passes each of them to the ConnectorManager


});